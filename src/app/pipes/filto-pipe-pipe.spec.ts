import { FiltoPipePipe } from './filto-pipe-pipe';

describe('FiltoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
